import 'package:flutter/material.dart';

class ArriveAtMatsumotoScreen extends StatelessWidget {
  const ArriveAtMatsumotoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('実装予定'));
  }
}
